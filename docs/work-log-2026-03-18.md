# CareVia 클로즈베타 전환 작업일지

## 날짜: 2026-03-18

---

## Phase 3: Mock 의존성 완전 제거 — Supabase 실데이터 전환

### 배경
- Phase 1(인증 기반)과 Phase 2(핵심 데이터 연동)가 완료된 상태
- 21개 파일이 여전히 `src/mocks/` 에서 mock 데이터 또는 타입을 import하고 있었음
- 목표: 모든 mock import 제거, 교사/보호자/관리자 전 영역 Supabase 실데이터 전환

### 작업 내역

#### Step 1: 공통 타입 파일 생성 (7개 신규)

| 파일 | 주요 export |
|------|------------|
| `src/types/admin.ts` | AdminUser, UserRole, UserStatus |
| `src/types/teacher.ts` | StudentDailyReport, ConditionLevel, MealLevel, BowelLevel, SleepLevel |
| `src/types/team.ts` | TeamMember, MemberRole, StudentAssignment, TeamMeeting, TeamActivity |
| `src/types/messages.ts` | ChatMessage, StudentConversation |
| `src/types/ai-care.ts` | AiCareReport, CareLevel, AiInsight, AiRecommendation, AiPatternItem |
| `src/types/reports.ts` | SentReport, REPORT_TYPES, ReportAttachment, ReportTypeKey |
| `src/types/behavior.ts` | StudentBehaviorStat, BehaviorSummary, BEHAVIOR_COLORS |

> `types/admin.ts`의 UserRole/UserStatus는 `types/profile.ts`의 동명 타입과 값이 다르므로 `index.ts`에서 re-export하지 않음

#### Step 2: Admin 컴포넌트 (4개) — 타입 import 경로 변경

| 파일 | 변경 내용 |
|------|----------|
| `admin-v2/page.tsx` | `mocks/adminUsers` → `types/admin`, 핸들러 id 타입 `number \| string` |
| `admin-v2/components/AdminOverview.tsx` | import 경로만 변경 |
| `admin-v2/components/PendingApprovals.tsx` | import 경로 + 핸들러 타입 수정 |
| `admin-v2/components/UserManagement.tsx` | import 경로 + 핸들러 타입 수정 |

#### Step 3: 교사 행동 추이 (3개) — Supabase 직접 조회

| 파일 | Before | After |
|------|--------|-------|
| `BehaviorSummaryCards.tsx` | mockBehaviorSummary | useTeacherData() behaviorStats에서 주간 통계 계산 |
| `BehaviorStudentRows.tsx` | mockStudentBehaviorStats | Supabase behavior_events 14일 조회 → 학생별 집계 |
| `BehaviorWeeklyChart.tsx` | mockWeeklyDailyData, mockBehaviorTypes | Supabase behavior_events → 요일별·학생별 차트 데이터 |

#### Step 4: 교사 케어톡 (4개) — Supabase parent_messages

| 파일 | Before | After |
|------|--------|-------|
| `TeacherMessages.tsx` | mockConversations | Supabase parent_messages 그룹 조회 → StudentConversation[] 구성 |
| `MessageChatPanel.tsx` | mock ChatMessage + quickReplies | types/messages 타입 + 로컬 quickReplies 상수 |
| `MessageStudentList.tsx` | mock StudentConversation 타입 | types/messages 타입 참조 |
| `MessageStudentStats.tsx` | mock StudentConversation 타입 | types/messages 타입 참조 |

#### Step 5: 교사 돌봄팀 (3개) — Supabase profiles + care_team

| 파일 | Before | After |
|------|--------|-------|
| `CareTeamView.tsx` | mockTeamMembers, mockStudentAssignments 등 | Supabase profiles 조회로 팀 구성, 미팅/활동 빈 상태 UI |
| `AddTeamMemberModal.tsx` | mockAdminUsers | Supabase profiles 조회로 교사 목록 |
| `EditAssignmentModal.tsx` | mock 타입만 | types/team 타입 참조, ID 타입 string \| number 호환 |

#### Step 6: 교사 기타 (3개)

| 파일 | Before | After |
|------|--------|-------|
| `StudentCard.tsx` | mockAiCareReports | AI 리포트 null (추후 ai_care_reports 테이블 연동 예정) |
| `AICareModal.tsx` | mock 타입만 | types/ai-care + types/teacher 타입 참조 |
| `ParentReports.tsx` | mockSentReports | Supabase parent_messages (message_type='daily_report') 조회 |

#### Step 7: 보호자 대시보드 (3개) — ParentDataContext 활용

| 파일 | Before | After |
|------|--------|-------|
| `HomeTimeline.tsx` | mockDailyStatus, mockTimelineEntries 등 5개 mock | ParentDataContext (todayRecord, messages, morningReports, behaviorEvents, careTeam) 조합 |
| `IncidentRoom.tsx` | mockCareTeamMessages, mockCareTeam 등 | ParentDataContext messages + careTeam 그룹핑 |
| `CorrelationChart.tsx` | mockBehaviorHistory30, mockMorningReportHistory | ParentDataContext behaviorEvents + morningReports → 30일 히스토리 계산 |

### 타입 호환성 수정

- Admin `id` 타입: `number` → `number | string` (Supabase UUID 호환)
- EditAssignmentModal: `assignedMemberIds` 타입 `number[]` → `(number | string)[]`
- MorningReportDB 필드 매핑: `sleep_time` (not `sleep`), `condition` enum → 한국어 변환
- Student 타입: `needs_attention` 없음 → `condition === 'bad' || 'very_bad'`로 계산

### 런타임 안전성 검증

- BehaviorStudentRows: 빈 이름 문자열 안전 처리 (`?? "?"`)
- TeacherMessages: conversations 빈 배열 시 early return
- IncidentRoom: careTeamMembers 빈 배열 시 `!member` 가드
- 모든 컴포넌트: loading / empty state UI 추가

---

## 검증 결과

| 항목 | 결과 |
|------|------|
| `npx tsc -b --noEmit` | ✅ 에러 0개 |
| `npx vite build` | ✅ 성공 (949ms) |
| Dev 서버 기동 | ✅ 정상 (210ms) |
| Mock import 잔존 | **0개** (21개 → 0개) |
| 변경 파일 수 | 28개 (+1,603 / -376) |

## Git 커밋

```
a0b6851 feat: 클로즈베타 Phase 3 — mock 의존성 완전 제거, Supabase 실데이터 전환
```

Push 완료: `03ebfc2..a0b6851 master -> master`

---

## 이전 Phase 이력

| 커밋 | 내용 |
|------|------|
| `ae7345a` | feat: 클로즈베타 전환 Phase 1+2 — Supabase Auth + mock→실데이터 연동 |
| `14c4a44` | fix: TypeScript 빌드 에러 수정 |
| `ff02ef8` | fix: ParentReports student undefined 크래시 방지 |
| `2cfa8f6` | fix: 데모 모드에서 Supabase fetch skip |
| `03ebfc2` | fix: 빈 데이터 시 차트/통계 컴포넌트 크래시 방지 |
| `a0b6851` | feat: 클로즈베타 Phase 3 — mock 의존성 완전 제거 |

---

## 남은 작업 / 향후 계획

- [ ] `src/mocks/` 디렉토리 삭제 (더 이상 import하는 파일 없음)
- [ ] AI 케어 리포트: `ai_care_reports` 테이블 연동 (StudentCard, AICareModal)
- [ ] 돌봄팀 미팅/활동 기록: Supabase 테이블 추가 필요
- [ ] 케어톡 weeklyStats: 학생별 주간 통계 계산 로직 보강
- [ ] Railway 배포 확인
